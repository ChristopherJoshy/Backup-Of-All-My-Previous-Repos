OPENROUTER_API_KEY=
OPENROUTER_MODEL=z-ai/glm-4.5-air:free
OPENROUTER_SITE_URL=http://localhost:3000
OPENROUTER_SITE_NAME=Orito

GEMINI_API_KEY=

SEARXNG_URL=http://localhost:8080
TAVILY_API_KEY=
SEARCH_PROVIDER=searxng

VECTOR_PROVIDER=pinecone
PINECONE_API_KEY=
PINECONE_INDEX=orito-rag
QDRANT_URL=http://localhost:6333

MONGODB_URI=mongodb://localhost:27017/orito

FIREBASE_PROJECT_ID=
FIREBASE_SERVICE_ACCOUNT_PATH=./firebase-credentials.json
JWT_SECRET=

GOOGLE_FORM_PRO_URL=

CORS_ORIGIN=http://localhost:3000

PORT=3001
NODE_ENV=development

# Super admin email - must be explicitly configured for admin privileges
# If not set, no user will automatically receive admin privileges
SUPER_ADMIN_EMAIL=your-email@example.com

# Performance & Caching (optimized for 300+ concurrent users)
# LLM response cache - stores responses to reduce API calls
CACHE_TTL_SECONDS=300
CACHE_MAX_ENTRIES=1000

# MongoDB connection pool
# Adjust based on server resources (recommended: 50-100 for production)
MONGODB_MAX_POOL_SIZE=100
MONGODB_MIN_POOL_SIZE=10
