services:
  - type: web
    name: wikirunai
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app:app --host 0.0.0.0 --port $PORT --ws websockets
    envVars:
      - key: PYTHON_VERSION
        value: "3.11"
